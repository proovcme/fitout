# FITOUT

Экономический симулятор генподрядной компании. Игрок набирает портфель офисных объектов, торгуется за бюджет и сроки, распределяет постоянный штат и подрядчиков, переживает кассовые разрывы и вручную руководит выбранной площадкой. Бригады автономно работают на процедурном 3D-объекте Three.js; остальные проекты рассчитываются тем же производственным движком в фоне.

Версия `0.1.0` вводит версионированное сохранение `GameSaveV2`, единый календарь компании и портфель до трёх активных объектов. У каждого объекта есть собственные график, договор, план-факт, материалы, документы, изменения заказчика и политика делегирования. Компания ведёт денежный журнал, дебиторку, кредиторку, зарплаты, резерв и 30-дневный кассовый прогноз; аванс остаётся финансированием объекта, а финальное закрытие блокируется исполнительной документацией. Постоянные сотрудники имеют процедурные биографии, навыки, зарплаты, стресс, выгорание и историю, назначаются только на один объект в день и физически появляются только там. Свободные специалисты остаются в живом штабе. Собственный офис теперь улучшается как внутренний проект, а не мгновенной случайной кнопкой.

Ригованные CC0 low-poly модели используются и на объектах, и в физическом главном меню организации. В штабе аватар и сотрудники ходят на скелетных анимациях, сохраняют цвета формы и настройки каски; прежние капсульные фигурки остаются только аварийным fallback на время загрузки. Численность бригад физически совпадает с карточками, усиление увеличивает темп, а избыток людей создаёт тесноту. Генератор собирает заказ из каталога 30 технологических операций: от обследования, проекта, укрытия и отдельных видов демонтажа до сетей, отделки, мебели, ИД и приёмки; чистое поле получает стройгородок, разбивку, фундаменты, каркас, фасад, кровлю и внешние сети.

Версия `0.0.7` добавила постоянную кнопку меню в верхнюю панель, усилила читаемость ходьбы и рабочих действий юнитов и заменила условные коробки во дворе на узнаваемый грузовик и сервисный фургон с кабинами, стёклами, фарами, колёсами и отдельными маршрутами движения.

Версия `0.0.6` добавила живой фон объекта: контекстные микроэпизоды между крупными событиями, производственные планёрки, замеры, доставки, проверки, споры и тесты инженерии. Они отображаются в 3D действиями людей, техникой, светом, инструментами и короткой плашкой состояния, но не требуют постоянного выбора игрока. Перегородки физически проходят стадию металлического каркаса до обшивки, камера мягко следует за активностью, а свет меняется в течение смены.

Первая учебная миссия, связанная кампания из четырёх заказов, развитие собственного офиса и физическая визуализация стадий остаются основой среза: доставка материалов, перегородки, инженерия, отделка, мебель, уборка и сдача. Юниты носят материалы, красят, сверлят, спорят, курят и простаивают; решения игрока сопровождаются визуальным импульсом, реакцией камеры и процедурным звуком.

В сборке есть 50 случайных событий и отдельный банк из 24 производственных ситуаций. Ситуация появляется знаком `!` над конкретным персонажем; игрок отвечает на вопрос подрядчика, а нанятый профильный специалист может решить часть вопросов самостоятельно. За день случается не больше двух крупных событий, иногда ни одного; один девятичасовой рабочий день длится пять реальных минут на скорости `1×`, а окна решений, чат и почта останавливают время.

Общий график генерируется по зависимостям и редактируется игроком; утренняя планёрка получает задачи из него, а завершённые работы больше не повторяются. Проект, исполнительная документация, зарплаты, материалы, мобилизация, аванс и этапные платежи участвуют в общей симуляции. Между объектами сохраняется экономика организации: прибыль и убытки, кредиты с процентами, долг, репутация и история проектов.

## Локальный запуск

```bash
npm install
npm run dev
```

Открыть `http://127.0.0.1:4173/fg/`.

## Проверки

Полная релизная проверка запускается одной командой. Она проверяет JavaScript и Python, прогоняет unit/integration-тесты, миграцию старого сохранения, production-сборку, аудит зависимостей и живой API на временной базе:

```bash
npm run test:release
```

Статическая production-сборка создаётся в `dist/`. Профили игроков, сохранения и история миссий обслуживаются небольшим API из `server/server.py` (production) или `server/server.mjs` (локальная Node.js-версия) по префиксу `/fg-api`.

## Production

Публичная версия: `https://fitout.ovc.me/`.

- Клиент разворачивается атомарно из версионированных каталогов `/var/www/fitout/releases/`.
- Текущий клиент подключён симлинком `/var/www/fitout/current`.
- API работает только на loopback-порту `4188` и публикуется Caddy по `/fg-api/`.
- Постоянные данные находятся в `/var/lib/fitout`, служба — `fitout-save.service`.

Атомарная выкладка с предварительными проверками, резервной копией базы игроков, перезапуском API, переключением симлинка и публичным smoke-тестом:

```bash
npm run deploy
```

По умолчанию используется `root@185.185.71.196`; другой SSH host задаётся через `FITOUT_SSH`. При ошибке после переключения скрипт возвращает предыдущий клиент и сервер. Отдельная проверка уже опубликованной версии: `npm run smoke:public`.
